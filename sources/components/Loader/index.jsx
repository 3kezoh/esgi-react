import React from "react";
import styles from "./loader.module.css";

/**
 * @description Displays a loader.
 * @returns {React.ReactElement} The component.
 */
function Loader() {
  return <div className={styles.loader} />;
}

export default Loader;
